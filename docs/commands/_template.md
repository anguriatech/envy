# envy <cmd>

<One-line summary of what the command does.>

## What it does

<1-2 sentences: what the command does and when to use it.>

## Aliases

| Alias | Notes |
|-------|-------|
| None | — |

(Or list the aliases, e.g. `ls`, `remove`, `unset`, `enc`, `dec`, `df`, `st`, `au`.)

## Syntax & flags

```text
envy <cmd> [OPTIONS]
```

| Flag | Description |
|------|-------------|
| `-e, --env <ENV>` | Target environment (default: `development`) |
| ... | ... |

## Examples

```bash
# <what this example demonstrates>
envy <cmd> ...
```

> **Note**: Examples use dummy values only — never commit real secrets.

## How it works

<Security notes: encryption, memory zeroing, keyring behavior, headless `ENVY_PASSPHRASE` behavior where relevant.>

**Exit codes**:

| Code | Meaning |
|------|---------|
| `0` | Success |
| `1` | ... |
| `2+` | Error (see the [exit-code reference](../../README.md#exit-codes)) |

## Related commands

- [envy <related>](envy-<related>.md) — <one line why related>
